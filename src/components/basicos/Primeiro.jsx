import React from "react";

export function Primeiro() {
    const msg = "Seja bem vindo(a)!"
    return (
        <div>
            <h1>Primeiro Componente</h1>
            <p>{msg}</p>
        </div>
    );
}
